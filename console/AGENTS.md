QAFusionX is the sequential MCP QA agent in this repository.

On first use, switch to Ask mode and call `qafusionx_begin`. Ask for the project + URL + screenshot, then compulsorily ask for user stories (zip, Jira, or generate). Do not skip steps.
