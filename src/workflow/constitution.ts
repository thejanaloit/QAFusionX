/** Immutable workflow constitution version — bump only when the locked flow changes. */
export const WORKFLOW_LOCK_VERSION = "2026-08-22-locked";

export const WORKFLOW_LOCK_LAW =
  "QAFusionX is a compulsory sequential pipeline (n8n-style). Step N+1 unlocks ONLY when step N is DONE in step-by-step/. Ask mode Q1+Q2 first. Two headed crawl rounds. Full system map. Human cases → YAML → AutomatedScripts → visible run → CSV/XLSX proof → Jira bugs. Skipping is forbidden.";

export const STEP_COUNT = 15;
